# Учёба — Сомов Никита Владимирович

ННГУ им. Лобачевского, Институт экономики, кафедра финансов и кредита,
3 курс, группа 3524Б6ЭКэп1. Папки верхнего уровня — дисциплины.

## Оформление отчётов

- Имя файла: `Сомов Н.В._3524Б6ЭКэп1_<задание>.docx` + одноимённый `.pdf`.
- Титул: «МИНИСТЕРСТВО НАУКИ И ВЫСШЕГО ОБРАЗОВАНИЯ РФ», ФГАОУ ВО «Национальный
  исследовательский Нижегородский государственный университет им. Н.И. Лобачевского»,
  «Институт экономики», «Кафедра финансов и кредита», «3 курс, группа 3524Б6ЭКэп1, Сомов Н.В.».
- Во всех практических заданиях нужны скриншоты ключевых действий — отчёт собирается
  из реальных снимков экрана, а не из пересказа.
- Работы однокурсника Нимченко П.С. лежат только как образец оформления — не копировать.

## Справочные информационные системы

`# Справочные информационные системы/scripts/` — всё, из чего собраны отчёты.

- `build21.py`, `build31.py`, `build32.py`, `build33.py`, `build41.py` — собирают отчёт
  по заданию из шаблона `Отчет 2022 СИС ЭБС.docx` и скриншотов; пишут docx в папку курса.
  Запуск: `python build32.py` из папки `scripts`. Нужен `python-docx`.
- `to_pdf.ps1` — docx → pdf через Word (только Windows). Без аргументов — все отчёты.
- Скриншоты: `shots*` — первая сессия (2.1, 3.1, 3.3, 4.1), `s31`/`s32`/`s32free`/`s33` —
  вторая (`s32free` — некоммерческая версия КонсультантПлюс, `s3x` — «КонсультантПлюс: Студент»).
- Остальные `.py` — скрипты съёмки (Playwright + Edge). Каркасы: `kpfree.py` — некоммерческая
  версия, `kp2.py`/`work32.py` — «Студент», `fav.py` — Избранное. Многие — разовые, как образец.

Съёмка: Playwright, `channel='msedge'`, viewport 1280×800, `device_scale_factor=2`;
в Word картинка шириной 16.5 см, у абзаца с картинкой обнулять отступы — иначе вылезает в поле.

### КонсультантПлюс

- Некоммерческая версия (consultant.ru/cons/cgi/online.cgi): полные тексты писем и многих
  актов — только по будням 20:00–24:00 МСК, в выходные круглосуточно. Аналитика
  (консультации, путеводители, «Корреспонденция счетов») недоступна вовсе.
- «КонсультантПлюс: Студент» (student2.consultant.ru): Избранное, закладки, папки, контроль.
  **Входит только сам Никита** — открыть видимое окно и ждать (`work32.run(job)`), логин
  и пароль не вводить. Сессия живёт ~15 минут: всю работу делать одним прогоном после входа.
- Разделы Избранного переключать кнопками `.x-page-favorites-sidebar-button`
  (0 — Закладки, 1 — Папки, 2 — Документы на контроле) и сверять заголовок
  `.x-page-favorites-title-toolbar__title`. Клик по координатам уже приводил к удалению
  не тех папок. Удаление папки удаляет и вложенные.

## Репозиторий

- Приватный: в отчётах персональные данные, в папке СИС — чужие работы. Публичным не делать.
- `scripts/kpprofile/` — браузерный профиль с авторизацией, в git не попадает.
- `gh` установлен в `C:\Program Files\GitHub CLI\gh.exe`, в PATH Git Bash его нет.
